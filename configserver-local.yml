server:
  port: 8888

spring:

  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/srikanthsara/microservice-config-repo.git
          default-label: main
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8761/eureka/